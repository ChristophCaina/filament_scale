name: ESPHome CI

on:
  push:
    branches: [ main ]
    tags:
      - '[0-9][0-9][0-9][0-9].*'
  pull_request:
    branches: [ main ]

jobs:
  # ── Validate config on every push / PR ──────────────────────
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install ESPHome
        run: pip install esphome

      - name: Validate ESPHome Config
        # Use ci-validate.yaml with local !include paths so PRs validate against current branch
        run: esphome config firmware/factory.yaml

  # ── Build & Release on tags only ────────────────────────────
  build-release:
    needs: validate
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/2')
    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Extract version from tag
        run: |
          echo "APP_VERSION=${{ github.ref_name }}" >> $GITHUB_ENV

      # ── esphome/build-action does everything automatically: ──
      #    - compiles firmware
      #    - finds firmware.factory.bin
      #    - generates manifest with MD5/SHA256
      #    - outputs folder named after the device
      - name: Build firmware
        id: esphome-build
        uses: esphome/build-action@v6
        with:
          yaml-file: firmware/factory.yaml
          complete-manifest: true
          release-url: https://github.com/${{ github.repository }}/releases/tag/${{ github.ref_name }}
          release-summary: "Filament Scale ${{ github.ref_name }}"

      - name: Show build outputs
        run: |
          echo "Build name: ${{ steps.esphome-build.outputs.name }}"
          echo "Build version: ${{ steps.esphome-build.outputs.version }}"
          ls -la "${{ steps.esphome-build.outputs.name }}/"

      # ── Rename files to fixed names for the installer ────────
      - name: Prepare release assets
        run: |
          BUILD_DIR="${{ steps.esphome-build.outputs.name }}"

          # Copy and rename factory binary for Web Installer
          cp "${BUILD_DIR}/"*.factory.bin ./filament-scale.bin

          # Copy and rename OTA binary for HA updates
          cp "${BUILD_DIR}/"*.ota.bin ./filament-scale.ota.bin

          # Fix binary path in manifest to match renamed file (both parts and ota)
          jq '.builds[0].parts[0].path = "filament-scale.bin" | .builds[0].ota.path = "filament-scale.ota.bin"' \
            "${BUILD_DIR}/manifest.json" > ./manifest-scale.json

          echo "Release assets prepared:"
          ls -la ./filament-scale.bin ./manifest-scale.json
          echo "Manifest content:"
          cat ./manifest-scale.json

      # ── Upload as GitHub Release assets ─────────────────────
      - name: Create Release and Upload Assets
        uses: softprops/action-gh-release@v2
        with:
          files: |
            ./filament-scale.bin
            ./filament-scale.ota.bin
            ./manifest-scale.json
          name: "Release ${{ env.APP_VERSION }}"
          body: |
            Automatischer Build für Version ${{ env.APP_VERSION }}.

            **Web Installer:** https://christophcaina.github.io/filament_scale/
          tag_name: ${{ github.ref_name }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}